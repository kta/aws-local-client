@AGENTS.md

## Claude Code

- 破壊的操作(force push・データ削除・本番反映)は実行前に必ず確認を取る。
- 「完了」と報告する前に、AGENTS.md の全チェックブロックを実際に実行して green を確認する。
- 仕様を変更したら、`docs/superpowers/specs/` と `e2e/SPEC-COVERAGE.md` を同じ変更内で同期する。
